---
ms.date: 02/03/2020
keywords: powershell,core
title: Problemas conhecidos do PowerShell 6.0
description: Este é um resumo de problemas conhecidos ou limitações no PowerShell 6
ms.openlocfilehash: 528315eff660167513045542227dce335355a7b8
ms.sourcegitcommit: 9080316e3ca4f11d83067b41351531672b667b7a
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/24/2020
ms.locfileid: "92501670"
---
# <a name="known-issues-for-powershell-60"></a><span data-ttu-id="98fdc-104">Problemas conhecidos do PowerShell 6.0</span><span class="sxs-lookup"><span data-stu-id="98fdc-104">Known Issues for PowerShell 6.0</span></span>

## <a name="known-issues-for-powershell-on-non-windows-platforms"></a><span data-ttu-id="98fdc-105">Problemas conhecidos do PowerShell em plataformas diferentes do Windows</span><span class="sxs-lookup"><span data-stu-id="98fdc-105">Known Issues for PowerShell on Non-Windows Platforms</span></span>

<span data-ttu-id="98fdc-106">As versões alfa do PowerShell no Linux e no macOS são em grande parte funcionais, mas têm limitações consideráveis e problemas de uso.</span><span class="sxs-lookup"><span data-stu-id="98fdc-106">Alpha releases of PowerShell on Linux and macOS are mostly functional but do have some significant limitations and usability issues.</span></span> <span data-ttu-id="98fdc-107">As versões beta do PowerShell no Linux e no macOS são mais funcionais e estáveis do que as versões alfa, mas ainda carecem de alguns recursos e podem conter bugs.</span><span class="sxs-lookup"><span data-stu-id="98fdc-107">Beta releases of PowerShell on Linux and macOS are more functional and stable than alpha releases, but still may be lacking some set of features, and can contain bugs.</span></span> <span data-ttu-id="98fdc-108">Em alguns casos, esses problemas são simplesmente bugs ainda não corrigidos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-108">In some cases, these issues are simply bugs that haven't been fixed yet.</span></span> <span data-ttu-id="98fdc-109">Em outros casos (como acontece com os aliases padrão para ls, cp etc)., queremos comentários da comunidade sobre as escolhas que fazemos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-109">In other cases (as with the default aliases for ls, cp, etc.), we are looking for feedback from the community regarding the choices we make.</span></span>

<span data-ttu-id="98fdc-110">Observação: devido a semelhanças dos vários subsistemas subjacentes, o PowerShell no Linux e no macOS tendem a compartilhar o mesmo nível de maturidade com relação a recursos e bugs.</span><span class="sxs-lookup"><span data-stu-id="98fdc-110">Note: Due to the similarities of many underlying subsystems, PowerShell on Linux and macOS tend to share the same level of maturity in both features and bugs.</span></span> <span data-ttu-id="98fdc-111">Com exceção dos itens abaixo, os problemas desta seção se aplicam a ambos os sistemas operacionais.</span><span class="sxs-lookup"><span data-stu-id="98fdc-111">Except as noted below, the issues in this section apply to both operating systems.</span></span>

### <a name="case-sensitivity-in-powershell"></a><span data-ttu-id="98fdc-112">Diferenciação de maiúsculas e minúsculas no PowerShell</span><span class="sxs-lookup"><span data-stu-id="98fdc-112">Case-sensitivity in PowerShell</span></span>

<span data-ttu-id="98fdc-113">Historicamente, o PowerShell nunca diferenciou maiúsculas de minúsculas, com algumas exceções.</span><span class="sxs-lookup"><span data-stu-id="98fdc-113">Historically, PowerShell has been uniformly case-insensitive, with few exceptions.</span></span> <span data-ttu-id="98fdc-114">Em sistemas operacionais do tipo UNIX, o sistema de arquivos é predominantemente indiferente a maiúsculas e minúsculas, e o PowerShell segue o padrão do sistema de arquivos. Isso é exposto de várias maneiras, óbvias e não óbvias.</span><span class="sxs-lookup"><span data-stu-id="98fdc-114">On UNIX-like operating systems, the file system is predominantly case-sensitive and PowerShell adheres to the standard of the file system; this is exposed through a number of ways, obvious and non-obvious.</span></span>

#### <a name="directly"></a><span data-ttu-id="98fdc-115">diretamente</span><span class="sxs-lookup"><span data-stu-id="98fdc-115">Directly</span></span>

- <span data-ttu-id="98fdc-116">Ao especificar um arquivo no PowerShell, a capitalização correta deve ser usada.</span><span class="sxs-lookup"><span data-stu-id="98fdc-116">When specifying a file in PowerShell, the correct case must be used.</span></span>

#### <a name="indirectly"></a><span data-ttu-id="98fdc-117">Indiretamente</span><span class="sxs-lookup"><span data-stu-id="98fdc-117">Indirectly</span></span>

- <span data-ttu-id="98fdc-118">Se um script tentar carregar um módulo, e o nome do módulo não estiver com as maiúsculas e minúsculas corretas, o carregamento do módulo falhará.</span><span class="sxs-lookup"><span data-stu-id="98fdc-118">If a script tries to load a module and the module name is not cased correctly, then the module load fails.</span></span> <span data-ttu-id="98fdc-119">Isso pode causar um problema com os scripts existentes, se o nome pelo qual o módulo é chamado não corresponder ao nome real do arquivo.</span><span class="sxs-lookup"><span data-stu-id="98fdc-119">This may cause a problem with existing scripts if the name by which the module is referenced doesn't match the actual filename.</span></span>
- <span data-ttu-id="98fdc-120">O preenchimento com Tab não ocorre automaticamente se a capitalização do nome do arquivo está incorreta.</span><span class="sxs-lookup"><span data-stu-id="98fdc-120">Tab-completion does not auto-complete if the filename case is wrong.</span></span> <span data-ttu-id="98fdc-121">O fragmento para preenchimento deve ter a capitalização correta.</span><span class="sxs-lookup"><span data-stu-id="98fdc-121">The fragment to complete must be cased properly.</span></span> <span data-ttu-id="98fdc-122">O preenchimento não diferencia maiúsculas de minúsculas para o nome do tipo e conclusões de membro do tipo.</span><span class="sxs-lookup"><span data-stu-id="98fdc-122">(Completion is case-insensitive for type name and type member completions.)</span></span>

### <a name="ps1-file-extensions"></a><span data-ttu-id="98fdc-123">Extensões de arquivo .PS1</span><span class="sxs-lookup"><span data-stu-id="98fdc-123">.PS1 File Extensions</span></span>

<span data-ttu-id="98fdc-124">Os scripts do PowerShell devem terminar em `.ps1` para o interpretador entender como carregar e executá-los no processo atual.</span><span class="sxs-lookup"><span data-stu-id="98fdc-124">PowerShell scripts must end in `.ps1` for the interpreter to understand how to load and run them in the current process.</span></span> <span data-ttu-id="98fdc-125">A execução de scripts no processo atual é o comportamento normal esperado para o PowerShell.</span><span class="sxs-lookup"><span data-stu-id="98fdc-125">Running scripts in the current process is the expected usual behavior for PowerShell.</span></span> <span data-ttu-id="98fdc-126">O número mágico `#!` pode ser adicionado a um script que não tem uma extensão `.ps1`, mas isso faz com que o script seja executado em uma nova instância do PowerShell, impedindo que o script funcione corretamente ao trocar objetos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-126">The `#!` magic number may be added to a script that doesn't have a `.ps1` extension, but this causes the script to be run in a new PowerShell instance preventing the script from working properly when interchanging objects.</span></span> <span data-ttu-id="98fdc-127">Observação: esse pode ser o comportamento desejável ao executar um script do PowerShell do `bash` ou outro shell.</span><span class="sxs-lookup"><span data-stu-id="98fdc-127">(Note: this may be the desirable behavior when executing a PowerShell script from `bash` or another shell.)</span></span>

### <a name="missing-command-aliases"></a><span data-ttu-id="98fdc-128">Aliases de comando ausentes</span><span class="sxs-lookup"><span data-stu-id="98fdc-128">Missing command aliases</span></span>

<span data-ttu-id="98fdc-129">No Linux/macOS, os "aliases de conveniência" para os comandos básicos `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` foram removidos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-129">On Linux/macOS, the "convenience aliases" for the basic commands `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` have been removed.</span></span> <span data-ttu-id="98fdc-130">No Windows, o PowerShell fornece um conjunto de aliases mapeados para nomes de comando do Linux para conveniência do usuário.</span><span class="sxs-lookup"><span data-stu-id="98fdc-130">On Windows, PowerShell provides a set of aliases that map to Linux command names for user convenience.</span></span> <span data-ttu-id="98fdc-131">Esses aliases foram removidos do PowerShell padrão em distribuições Linux/macOS, permitindo a execução do executável nativo sem especificar um caminho.</span><span class="sxs-lookup"><span data-stu-id="98fdc-131">These aliases have been removed from the default PowerShell on Linux/macOS distributions, allowing the native executable to be run without specifying a path.</span></span>

<span data-ttu-id="98fdc-132">Há vantagens e desvantagens nisso.</span><span class="sxs-lookup"><span data-stu-id="98fdc-132">There are pros and cons to doing this.</span></span> <span data-ttu-id="98fdc-133">A remoção dos aliases expõe a experiência do comando nativo ao usuário do PowerShell, mas reduz a funcionalidade no shell, pois os comandos nativos retornam cadeias de caracteres em vez de objetos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-133">Removing the aliases exposes the native command experience to the PowerShell user, but reduces functionality in the shell because the native commands return strings instead of objects.</span></span>

> [!NOTE]
> <span data-ttu-id="98fdc-134">Essa é uma área sobre a qual a equipe do PowerShell está procurando comentários.</span><span class="sxs-lookup"><span data-stu-id="98fdc-134">This is an area where the PowerShell team is looking for feedback.</span></span> <span data-ttu-id="98fdc-135">Qual é a solução preferida?</span><span class="sxs-lookup"><span data-stu-id="98fdc-135">What is the preferred solution?</span></span>
> <span data-ttu-id="98fdc-136">Devemos deixar como está ou adicionar novamente os aliases de conveniência?</span><span class="sxs-lookup"><span data-stu-id="98fdc-136">Should we leave it as is or add the convenience aliases back?</span></span> <span data-ttu-id="98fdc-137">Consulte a [Edição #929](https://github.com/PowerShell/PowerShell/issues/929).</span><span class="sxs-lookup"><span data-stu-id="98fdc-137">See [Issue #929](https://github.com/PowerShell/PowerShell/issues/929).</span></span>

### <a name="missing-wildcard-globbing-support"></a><span data-ttu-id="98fdc-138">Falta o suporte ao recurso de curinga</span><span class="sxs-lookup"><span data-stu-id="98fdc-138">Missing Wildcard (globbing) Support</span></span>

<span data-ttu-id="98fdc-139">No momento, o PowerShell só faz expansão de curinga (recurso de curinga) para cmdlets internos do Windows, e para comandos externos ou binários, bem como os cmdlets no Linux.</span><span class="sxs-lookup"><span data-stu-id="98fdc-139">Currently, PowerShell only does wildcard expansion (globbing) for built-in cmdlets on Windows, and for external commands or binaries as well as cmdlets on Linux.</span></span> <span data-ttu-id="98fdc-140">Isso significa que um comando como `ls
*.txt` falha devido à falta de expansão do asterisco para corresponder aos nomes de arquivo.</span><span class="sxs-lookup"><span data-stu-id="98fdc-140">This means that a command like `ls
*.txt` fails because the asterisk is not expanded to match filenames.</span></span> <span data-ttu-id="98fdc-141">Você pode contornar esse problema fazendo `ls (gci *.txt | % name)` ou, simplificando, `gci *.txt` usando o equivalente interno do PowerShell para `ls`.</span><span class="sxs-lookup"><span data-stu-id="98fdc-141">You can work around this by doing `ls (gci *.txt | % name)` or, more simply, `gci *.txt` using the PowerShell built-in equivalent to `ls`.</span></span>

<span data-ttu-id="98fdc-142">Confira [#954](https://github.com/PowerShell/PowerShell/issues/954) para fazer comentários sobre como melhorar a experiência de recurso de curinga no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="98fdc-142">See [#954](https://github.com/PowerShell/PowerShell/issues/954) to give us feedback on how to improve the globbing experience on Linux/macOS.</span></span>

### <a name="net-framework-vs-net-core-framework"></a><span data-ttu-id="98fdc-143">.NET Framework versus .NET Core Framework</span><span class="sxs-lookup"><span data-stu-id="98fdc-143">.NET Framework vs .NET Core Framework</span></span>

<span data-ttu-id="98fdc-144">O PowerShell no Linux/macOS usa .NET Core, que é um subconjunto do .NET Framework completo no Microsoft Windows.</span><span class="sxs-lookup"><span data-stu-id="98fdc-144">PowerShell on Linux/macOS uses .NET Core which is a subset of the full .NET Framework on Microsoft Windows.</span></span> <span data-ttu-id="98fdc-145">Isso é significativo, pois o PowerShell fornece acesso direto aos tipos de estrutura subjacentes, métodos etc. Como resultado, scripts executados no Windows não podem ser executados em outras plataformas devido às diferenças nas estruturas.</span><span class="sxs-lookup"><span data-stu-id="98fdc-145">This is significant because PowerShell provides direct access to the underlying framework types, methods, etc. As a result, scripts that run on Windows may not run on non-Windows platforms because of the differences in the frameworks.</span></span> <span data-ttu-id="98fdc-146">Para saber mais sobre o .NET Core Framework, confira [dotnetfoundation.org](https://dotnetfoundation.org/).</span><span class="sxs-lookup"><span data-stu-id="98fdc-146">For more information about .NET Core Framework, see [dotnetfoundation.org](https://dotnetfoundation.org/).</span></span>

<span data-ttu-id="98fdc-147">Com o surgimento do [.NET Standard 2.0](https://devblogs.microsoft.com/dotnet/introducing-net-standard/), o .NET Core 2.0 traz de volta muitos tipos e métodos tradicionais presentes no .NET Framework completo.</span><span class="sxs-lookup"><span data-stu-id="98fdc-147">With the advent of [.NET Standard 2.0](https://devblogs.microsoft.com/dotnet/introducing-net-standard/), .NET Core 2.0 brings back many of the traditional types and methods present in the full .NET Framework.</span></span> <span data-ttu-id="98fdc-148">Isso significa que o PowerShell Core pode carregar vários módulos tradicionais do Windows PowerShell sem modificação.</span><span class="sxs-lookup"><span data-stu-id="98fdc-148">This means that PowerShell Core can load many traditional Windows PowerShell modules without modification.</span></span> <span data-ttu-id="98fdc-149">Siga o nosso trabalho relacionado ao .NET Standard 2.0 [aqui](https://github.com/PowerShell/PowerShell/projects/4).</span><span class="sxs-lookup"><span data-stu-id="98fdc-149">You can follow our .NET Standard 2.0 related work [here](https://github.com/PowerShell/PowerShell/projects/4).</span></span>

### <a name="redirection-issues"></a><span data-ttu-id="98fdc-150">Redirecionamento de problemas</span><span class="sxs-lookup"><span data-stu-id="98fdc-150">Redirection Issues</span></span>

<span data-ttu-id="98fdc-151">Não há suporte para o redirecionamento de entrada no PowerShell em qualquer plataforma.</span><span class="sxs-lookup"><span data-stu-id="98fdc-151">Input redirection is not supported in PowerShell on any platform.</span></span>
[<span data-ttu-id="98fdc-152">Edição #1629</span><span class="sxs-lookup"><span data-stu-id="98fdc-152">Issue #1629</span></span>](https://github.com/PowerShell/PowerShell/issues/1629)

<span data-ttu-id="98fdc-153">Use `Get-Content` para gravar o conteúdo de um arquivo no pipeline.</span><span class="sxs-lookup"><span data-stu-id="98fdc-153">Use `Get-Content` to write the contents of a file into the pipeline.</span></span>

<span data-ttu-id="98fdc-154">A saída redirecionada contém a BOM (marca de ordem de byte) Unicode quando a codificação UTF-8 padrão for usada.</span><span class="sxs-lookup"><span data-stu-id="98fdc-154">Redirected output contains the Unicode byte order mark (BOM) when the default UTF-8 encoding is used.</span></span> <span data-ttu-id="98fdc-155">A BOM causa problemas ao trabalhar com utilitários que não a esperam, ou ao ser anexada a um arquivo.</span><span class="sxs-lookup"><span data-stu-id="98fdc-155">The BOM causes problems when working with utilities that do not expect it or when appending to a file.</span></span> <span data-ttu-id="98fdc-156">Use `-Encoding Ascii` para gravar texto ASCII, que não tem uma BOM.</span><span class="sxs-lookup"><span data-stu-id="98fdc-156">Use `-Encoding Ascii` to write ASCII text, which does not have a BOM.</span></span>

> [!Note]
> <span data-ttu-id="98fdc-157">Confira [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) para nos enviar comentários sobre como melhorar a experiência de codificação no PowerShell Core em todas as plataformas.</span><span class="sxs-lookup"><span data-stu-id="98fdc-157">see [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) to give us feedback on improving the encoding experience for PowerShell Core across all platforms.</span></span> <span data-ttu-id="98fdc-158">Estamos trabalhando para dar suporte a UTF-8 sem uma BOM e possivelmente alterando os padrões de codificação para diversos cmdlets entre plataformas.</span><span class="sxs-lookup"><span data-stu-id="98fdc-158">We are working to support UTF-8 without a BOM and potentially changing the encoding defaults for various cmdlets across platforms.</span></span>

### <a name="job-control"></a><span data-ttu-id="98fdc-159">Controle de trabalho</span><span class="sxs-lookup"><span data-stu-id="98fdc-159">Job Control</span></span>

<span data-ttu-id="98fdc-160">Não há nenhum suporte de controle de trabalho do PowerShell no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="98fdc-160">There is no job-control support in PowerShell on Linux/macOS.</span></span>
<span data-ttu-id="98fdc-161">Os comandos `fg` e `bg` não estão disponíveis.</span><span class="sxs-lookup"><span data-stu-id="98fdc-161">The `fg` and `bg` commands are not available.</span></span>

<span data-ttu-id="98fdc-162">Por enquanto, você pode usar os [Trabalhos do PowerShell](/powershell/module/microsoft.powershell.core/about/about_jobs) que funcionam em todas as plataformas.</span><span class="sxs-lookup"><span data-stu-id="98fdc-162">For the time being, you can use [PowerShell jobs](/powershell/module/microsoft.powershell.core/about/about_jobs) which do work across all platforms.</span></span>

### <a name="remoting-support"></a><span data-ttu-id="98fdc-163">Suporte à comunicação remota</span><span class="sxs-lookup"><span data-stu-id="98fdc-163">Remoting Support</span></span>

<span data-ttu-id="98fdc-164">Atualmente, o PowerShell Core dá suporte à Comunicação Remota do PowerShell (PSRP) em WSMan com a Autenticação básica no macOS e Linux e com a autenticação baseada em NTLM no Linux.</span><span class="sxs-lookup"><span data-stu-id="98fdc-164">Currently, PowerShell Core supports PowerShell Remoting (PSRP) over WSMan with Basic authentication on macOS and Linux, and with NTLM-based authentication on Linux.</span></span> <span data-ttu-id="98fdc-165">Não há suporte para a autenticação baseada em Kerberos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-165">(Kerberos-based authentication is not supported.)</span></span>

<span data-ttu-id="98fdc-166">O trabalho para comunicação remota baseada em WSMan está sendo feito no repositório [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider).</span><span class="sxs-lookup"><span data-stu-id="98fdc-166">The work for WSMan-based remoting is being done in the [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider) repo.</span></span>

<span data-ttu-id="98fdc-167">O PowerShell Core também oferece suporte à Comunicação Remota do PowerShell (PSRP) sobre SSH em todas as plataformas (Windows, macOS e Linux).</span><span class="sxs-lookup"><span data-stu-id="98fdc-167">PowerShell Core also supports PowerShell Remoting (PSRP) over SSH on all platforms (Windows, macOS, and Linux).</span></span> <span data-ttu-id="98fdc-168">Embora isso não tenha suporte atualmente em produção, você pode aprender mais sobre como configurar isso [aqui](../learn/remoting/SSH-Remoting-in-PowerShell-Core.md).</span><span class="sxs-lookup"><span data-stu-id="98fdc-168">While this is not currently supported in production, you can learn more about setting this up [here](../learn/remoting/SSH-Remoting-in-PowerShell-Core.md).</span></span>

### <a name="just-enough-administration-jea-support"></a><span data-ttu-id="98fdc-169">Suporte a JEA (Administração Just Enough)</span><span class="sxs-lookup"><span data-stu-id="98fdc-169">Just-Enough-Administration (JEA) Support</span></span>

<span data-ttu-id="98fdc-170">A capacidade de criar pontos de extremidade de comunicação remota de administração restrita (JEA) não está disponível no PowerShell no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="98fdc-170">The ability to create constrained administration (JEA) remoting endpoints is not currently available in PowerShell on Linux/macOS.</span></span> <span data-ttu-id="98fdc-171">No momento, esse recurso não está no escopo para 6.0 e é algo que consideraremos após o 6.0, pois requer um trabalho considerável de design.</span><span class="sxs-lookup"><span data-stu-id="98fdc-171">This feature is currently not in scope for 6.0 and something we will consider post 6.0 as it requires significant design work.</span></span>

### <a name="sudo-exec-and-powershell"></a><span data-ttu-id="98fdc-172">`sudo`, `exec` e PowerShell</span><span class="sxs-lookup"><span data-stu-id="98fdc-172">`sudo`, `exec`, and PowerShell</span></span>

<span data-ttu-id="98fdc-173">Como o PowerShell executa a maioria dos comandos na memória (como Python ou Ruby), não é possível usar o sudo diretamente com integrações do PowerShell. Você pode, obviamente, executar `pwsh` com o sudo. Se for necessário executar um cmdlet do PowerShell de dentro do PowerShell com sudo, por exemplo, `sudo Set-Date 8/18/2016`, você faria `sudo pwsh Set-Date 8/18/2016`.</span><span class="sxs-lookup"><span data-stu-id="98fdc-173">Because PowerShell runs most commands in memory (like Python or Ruby), you can't use sudo directly with PowerShell built-ins. (You can, of course, run `pwsh` from sudo.) If it is necessary to run a PowerShell cmdlet from within PowerShell with sudo, for example, `sudo Set-Date 8/18/2016`, then you would do `sudo pwsh Set-Date 8/18/2016`.</span></span> <span data-ttu-id="98fdc-174">Da mesma forma, não é possível executar uma integração do PowerShell diretamente.</span><span class="sxs-lookup"><span data-stu-id="98fdc-174">Likewise, you can't exec a PowerShell built-in directly.</span></span> <span data-ttu-id="98fdc-175">Em vez disso, será necessário fazer `exec pwsh item_to_exec`.</span><span class="sxs-lookup"><span data-stu-id="98fdc-175">Instead you would have to do `exec pwsh item_to_exec`.</span></span>

<span data-ttu-id="98fdc-176">Este problema está sendo acompanhado como parte da [#3232](https://github.com/PowerShell/PowerShell/issues/3232).</span><span class="sxs-lookup"><span data-stu-id="98fdc-176">This issue is being tracked as part of [#3232](https://github.com/PowerShell/PowerShell/issues/3232).</span></span>

### <a name="missing-cmdlets"></a><span data-ttu-id="98fdc-177">Cmdlets ausentes</span><span class="sxs-lookup"><span data-stu-id="98fdc-177">Missing Cmdlets</span></span>

<span data-ttu-id="98fdc-178">Muitos comandos (cmdlets) normalmente disponíveis no PowerShell não está disponíveis no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="98fdc-178">A large number of the commands (cmdlets) normally available in PowerShell are not available on Linux/macOS.</span></span> <span data-ttu-id="98fdc-179">Em muitos casos, esses comandos não fazem nenhum sentido nessas plataformas (por exemplo, recursos específicos do Windows como o Registro).</span><span class="sxs-lookup"><span data-stu-id="98fdc-179">In many cases, these commands make no sense on these platforms (e.g. Windows-specific features like the registry).</span></span> <span data-ttu-id="98fdc-180">Outros comandos, como os comandos de controle de serviço (Get/Start/Stop-Service) estão presentes, mas não são funcionais.</span><span class="sxs-lookup"><span data-stu-id="98fdc-180">Other commands like the service control commands (Get/Start/Stop-Service) are present, but not functional.</span></span> <span data-ttu-id="98fdc-181">As versões futuras podem corrigir esses problemas arrumando os cmdlets quebrados e adicionando novos valores ao longo do tempo.</span><span class="sxs-lookup"><span data-stu-id="98fdc-181">Future releases may correct these problems by fixing the broken cmdlets and adding new ones over time.</span></span>

### <a name="command-availability"></a><span data-ttu-id="98fdc-182">Disponibilidade de comando</span><span class="sxs-lookup"><span data-stu-id="98fdc-182">Command Availability</span></span>

<span data-ttu-id="98fdc-183">A tabela a seguir lista os comandos que não funcionam no PowerShell no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="98fdc-183">The following table lists commands that are known not to work in PowerShell on Linux/macOS.</span></span>

|<span data-ttu-id="98fdc-184">Comandos</span><span class="sxs-lookup"><span data-stu-id="98fdc-184">Commands</span></span>|<span data-ttu-id="98fdc-185">Estado Operacional</span><span class="sxs-lookup"><span data-stu-id="98fdc-185">Operational State</span></span>|<span data-ttu-id="98fdc-186">Anotações</span><span class="sxs-lookup"><span data-stu-id="98fdc-186">Notes</span></span>|
|--------|-----------------|-----|
|<span data-ttu-id="98fdc-187">`Get-Service`, `New-Service`, `Restart-Service`, `Resume-Service`, `Set-Service`, `Start-Service`, `Stop-Service`, `Suspend-Service`</span><span class="sxs-lookup"><span data-stu-id="98fdc-187">`Get-Service`, `New-Service`, `Restart-Service`, `Resume-Service`, `Set-Service`, `Start-Service`, `Stop-Service`, `Suspend-Service`</span></span>|<span data-ttu-id="98fdc-188">Não disponível.</span><span class="sxs-lookup"><span data-stu-id="98fdc-188">Not Available.</span></span>|<span data-ttu-id="98fdc-189">Esses comandos não são reconhecidos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-189">These commands are not recognized.</span></span> <span data-ttu-id="98fdc-190">Isso deve ser corrigido em uma versão futura.</span><span class="sxs-lookup"><span data-stu-id="98fdc-190">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="98fdc-191">`Get-Acl`, `Get-AuthenticodeSignature`, `Get-CmsMessage`, `New-FileCatalog`, `Protect-CmsMessage`, `Set-Acl`, `Set-AuthenticodeSignature`, `Test-FileCatalog`, `Unprotect-CmsMessage`</span><span class="sxs-lookup"><span data-stu-id="98fdc-191">`Get-Acl`, `Get-AuthenticodeSignature`, `Get-CmsMessage`, `New-FileCatalog`, `Protect-CmsMessage`, `Set-Acl`, `Set-AuthenticodeSignature`, `Test-FileCatalog`, `Unprotect-CmsMessage`</span></span>|<span data-ttu-id="98fdc-192">Não disponível.</span><span class="sxs-lookup"><span data-stu-id="98fdc-192">Not available.</span></span>|<span data-ttu-id="98fdc-193">Esses comandos não são reconhecidos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-193">These commands are not recognized.</span></span> <span data-ttu-id="98fdc-194">Isso deve ser corrigido em uma versão futura.</span><span class="sxs-lookup"><span data-stu-id="98fdc-194">This should be fixed in a future release.</span></span>|
|`Wait-Process`|<span data-ttu-id="98fdc-195">Disponível, não funciona corretamente.</span><span class="sxs-lookup"><span data-stu-id="98fdc-195">Available, doesn't work properly.</span></span> |<span data-ttu-id="98fdc-196">Por exemplo, `Start-Process gvim -PassThru | Wait-Process` não funciona; ele falha ao aguardar o processo.</span><span class="sxs-lookup"><span data-stu-id="98fdc-196">For example `Start-Process gvim -PassThru | Wait-Process` doesn't work; it fails to wait for the process.</span></span>|
|<span data-ttu-id="98fdc-197">`Connect-PSSession`, `Disable-PSRemoting`, `Disable-PSSessionConfiguration`, `Disconnect-PSSession`, `Enable-PSRemoting`, `Enable-PSSessionConfiguration`, `Get-PSSessionCapability`, `Get-PSSessionConfiguration`, `New-PSSessionConfigurationFile`, `Receive-PSSession`, `Register-PSSessionConfiguration`, `Set-PSSessionConfiguration`, `Test-PSSessionConfigurationFile`, `Unregister-PSSessionConfiguration`</span><span class="sxs-lookup"><span data-stu-id="98fdc-197">`Connect-PSSession`, `Disable-PSRemoting`, `Disable-PSSessionConfiguration`, `Disconnect-PSSession`, `Enable-PSRemoting`, `Enable-PSSessionConfiguration`, `Get-PSSessionCapability`, `Get-PSSessionConfiguration`, `New-PSSessionConfigurationFile`, `Receive-PSSession`, `Register-PSSessionConfiguration`, `Set-PSSessionConfiguration`, `Test-PSSessionConfigurationFile`, `Unregister-PSSessionConfiguration`</span></span>|<span data-ttu-id="98fdc-198">Não disponível.</span><span class="sxs-lookup"><span data-stu-id="98fdc-198">Not Available.</span></span>|<span data-ttu-id="98fdc-199">Esses comandos não são reconhecidos.</span><span class="sxs-lookup"><span data-stu-id="98fdc-199">These commands are not recognized.</span></span> <span data-ttu-id="98fdc-200">Isso deve ser corrigido em uma versão futura.</span><span class="sxs-lookup"><span data-stu-id="98fdc-200">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="98fdc-201">`Get-Event`, `New-Event`, `Register-EngineEvent`, `Remove-Event`, `Unregister-Event`</span><span class="sxs-lookup"><span data-stu-id="98fdc-201">`Get-Event`, `New-Event`, `Register-EngineEvent`, `Remove-Event`, `Unregister-Event`</span></span>|<span data-ttu-id="98fdc-202">Disponível, mas não há fontes de evento disponíveis.</span><span class="sxs-lookup"><span data-stu-id="98fdc-202">Available but no event sources are available.</span></span>|<span data-ttu-id="98fdc-203">Os comandos de evento do PowerShell estão presentes, mas a maioria das fontes de evento usadas com os comandos (como System.Timers.Timer) não está disponível no Linux, tornando os comandos inúteis na versão alfa.</span><span class="sxs-lookup"><span data-stu-id="98fdc-203">The PowerShell eventing commands are present but most of the event sources used with the commands (such as System.Timers.Timer) are not available on Linux making the commands useless in the Alpha release.</span></span>|
|`Set-ExecutionPolicy`|<span data-ttu-id="98fdc-204">Disponível, mas não funciona.</span><span class="sxs-lookup"><span data-stu-id="98fdc-204">Available but doesn't work.</span></span>|<span data-ttu-id="98fdc-205">Retorna uma mensagem informando que não há suporte nessa plataforma.</span><span class="sxs-lookup"><span data-stu-id="98fdc-205">Returns a message saying not supported on this platform.</span></span> <span data-ttu-id="98fdc-206">A política de execução é um "dispositivo de segurança" voltado ao usuário que o ajuda a não cometer erros caros.</span><span class="sxs-lookup"><span data-stu-id="98fdc-206">Execution policy is a user-focused "safety belt" that helps prevent the user from making expensive mistakes.</span></span> <span data-ttu-id="98fdc-207">Não é um limite de segurança.</span><span class="sxs-lookup"><span data-stu-id="98fdc-207">It is not a security boundary.</span></span>|
|<span data-ttu-id="98fdc-208">`New-PSSessionOption`, `New-PSTransportOption`</span><span class="sxs-lookup"><span data-stu-id="98fdc-208">`New-PSSessionOption`, `New-PSTransportOption`</span></span>|<span data-ttu-id="98fdc-209">Disponível, mas `New-PSSession` não funciona.</span><span class="sxs-lookup"><span data-stu-id="98fdc-209">Available but `New-PSSession` doesn't work.</span></span>|<span data-ttu-id="98fdc-210">`New-PSSessionOption` e `New-PSTransportOption` não estão atualmente verificados para funcionar agora que `New-PSSession` funciona.</span><span class="sxs-lookup"><span data-stu-id="98fdc-210">`New-PSSessionOption` and `New-PSTransportOption` are not currently verified to work now that `New-PSSession` works.</span></span>|
